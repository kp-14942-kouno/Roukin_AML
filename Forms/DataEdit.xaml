<Window x:Class="MyTemplate.Forms.DataEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyTemplate.Forms"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded"
        Title="データ編集" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="34"/>
        </Grid.RowDefinitions>

        <ScrollViewer Name="sv_Edit" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="sp_Edit" Background="WhiteSmoke" Margin="10"/>
        </ScrollViewer>

        <Button x:Name="bt_Save" Content="保存" Grid.Row="1" Grid.Column="1" Margin="3" Foreground="Red" IsTabStop="False" Click="bt_Save_Click"/>
        <Button x:Name="bt_Close" Content="閉じる" Grid.Row="1" Grid.Column="2" Margin="3" IsTabStop="False" Click="bt_Close_Click"/>

    </Grid>
</Window>
