<UserControl x:Class="MyTemplate.Report.Views.SinseishoMeisai"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyTemplate.Report.Views"
             mc:Ignorable="d" 
             UseLayoutRounding="True"
             d:DesignHeight="1123" d:DesignWidth="795">
    <Grid Margin="1cm" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5cm"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="1.5cm"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="tb_BankName" FontSize="24" TextAlignment="Left" VerticalAlignment="Center"/>

        <StackPanel Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!--ヘッダー-->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="125"/>
                        <ColumnDefinition Width="275"/>
                        <ColumnDefinition Width="125"/>
                    </Grid.ColumnDefinitions>

                    <Border BorderBrush="Black" BorderThickness="1,1,1,1" Grid.Column="0" SnapsToDevicePixels="True">
                        <TextBlock Text="No." Padding="4" TextAlignment="Center"/>
                    </Border>

                    <Border BorderBrush="Black" BorderThickness="0,1,1,1" Grid.Column="1" SnapsToDevicePixels="True">
                        <TextBlock Text="人格コード" Padding="4" TextAlignment="Center"/>
                    </Border>

                    <Border BorderBrush="Black" BorderThickness="0,1,1,1" Grid.Column="2" SnapsToDevicePixels="True">
                        <TextBlock Text="顧客番号" Padding="4" TextAlignment="Center"/>
                    </Border>

                    <Border BorderBrush="Black" BorderThickness="0,1,1,1" Grid.Column="3" SnapsToDevicePixels="True">
                        <TextBlock Text="団体名" Padding="4" TextAlignment="Center"/>
                    </Border>

                    <Border BorderBrush="Black" BorderThickness="0,1,1,1" Grid.Column="4" SnapsToDevicePixels="True">
                        <TextBlock Text="BPO管理番号" Padding="4" TextAlignment="Center"/>
                    </Border>
                </Grid>

                <!--データ行-->
                <ItemsControl Grid.Row="1" x:Name="ItemList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="125"/>
                                    <ColumnDefinition Width="275"/>
                                    <ColumnDefinition Width="125"/>
                                </Grid.ColumnDefinitions>

                                <Border BorderBrush="Black" BorderThickness="1,0,1,1" Grid.Column="0" SnapsToDevicePixels="True">
                                    <TextBlock Text="{Binding no}" Padding="4" TextAlignment="Center"/>
                                </Border>

                                <Border BorderBrush="Black" BorderThickness="0,0,1,1" Grid.Column="1" SnapsToDevicePixels="True">
                                    <TextBlock Text="{Binding bpo_persona_cd}" Padding="4" TextAlignment="Center"/>
                                </Border>

                                <Border BorderBrush="Black" BorderThickness="0,0,1,1" Grid.Column="2" SnapsToDevicePixels="True">
                                    <TextBlock Text="{Binding bpo_cust_no}" Padding="4" TextAlignment="Center"/>
                                </Border>

                                <Border BorderBrush="Black" BorderThickness="0,0,1,1" Grid.Column="3" SnapsToDevicePixels="True">
                                    <TextBlock Text="{Binding bpo_org_kanji}" Padding="4" TextAlignment="Center"/>
                                </Border>

                                <Border BorderBrush="Black" BorderThickness="0,0,1,1" Grid.Column="4" SnapsToDevicePixels="True">
                                    <TextBlock Text="{Binding bpo_num}" Padding="4" TextAlignment="Center"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </StackPanel>
        
        <TextBlock x:Name="tb_Pages" Text="ページ数：" FontSize="16" Grid.Row="2" Margin="0,0,0.5cm,0" VerticalAlignment="Center" HorizontalAlignment="Right"/>
    </Grid>
</UserControl>
